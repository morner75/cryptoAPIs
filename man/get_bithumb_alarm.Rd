% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bithumb.R
\name{get_bithumb_alarm}
\alias{get_bithumb_alarm}
\title{Retrieve Bithumb market risk alarm data}
\usage{
get_bithumb_alarm(quote = NULL, verbose = FALSE)
}
\arguments{
\item{quote}{Character. Optional quote-currency filter (`"KRW"`).
If `NULL` (default), all markets are returned.}

\item{verbose}{Logical. Print progress messages (default `FALSE`).}
}
\value{
A [tibble::tibble()] with columns `market` (`"ASSET-QUOTE"`),
  `symbol` (Bithumb native), `alarm` (`"주의"` / `"경고"` / `"위험"`),
  and `reason` (`" / "`-delimited when multiple warning types apply).
}
\description{
Queries the Bithumb virtual asset warning API and returns markets with
active warnings, consolidating multiple warning types per market into a
single row showing the highest alarm tier.
}
\examples{
\dontrun{
get_bithumb_alarm(quote = "KRW", verbose = TRUE)
}

}
