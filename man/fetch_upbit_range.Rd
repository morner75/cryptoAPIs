% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upbit.R
\name{fetch_upbit_range}
\alias{fetch_upbit_range}
\title{Fetch a date-range of OHLCV candles from Upbit}
\usage{
fetch_upbit_range(market, from, to, unit = "min")
}
\arguments{
\item{market}{Character. Market in `"ASSET-QUOTE"` format (e.g., `"BTC-KRW"`).}

\item{from}{POSIXct or Date. Start of the range (inclusive).}

\item{to}{POSIXct or Date. End of the range (inclusive).}

\item{unit}{Character. Candle unit: `"min"` (1-minute), `"hour"` (60-minute),
or `"day"`. Defaults to `"min"`.}
}
\value{
A [data.frame] with columns `time_kst`, `opening_price`,
  `high_price`, `low_price`, `trade_price`, `volume`, sorted by `time_kst`
  with duplicates removed. Returns `NULL` on error or when no data is
  available.
}
\description{
Paginates through the Upbit candles endpoint to retrieve all 1-minute (or
hourly / daily) bars between `from` and `to`.
}
\examples{
\dontrun{
fetch_upbit_range(
  "BTC-KRW",
  from = as.POSIXct("2024-01-01 00:00:00", tz = "Asia/Seoul"),
  to   = as.POSIXct("2024-01-01 01:00:00", tz = "Asia/Seoul")
)
}

}
