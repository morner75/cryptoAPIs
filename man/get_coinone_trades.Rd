% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coinone.R
\name{get_coinone_trades}
\alias{get_coinone_trades}
\title{Fetch recent trade tick data from Coinone}
\usage{
get_coinone_trades(market, from, to)
}
\arguments{
\item{market}{Character. Market in `"ASSET-QUOTE"` format (e.g., `"BTC-KRW"`).}

\item{from}{POSIXct or Date. Start of the range (inclusive).}

\item{to}{POSIXct or Date. End of the range (inclusive).}
}
\value{
A [data.frame] with columns `time_kst`, `trade_price`, `volume`,
  `ask_bid` (`"ASK"` = seller-initiated / `"BID"` = buyer-initiated),
  `sequential_id`, sorted by `time_kst`. Returns `NULL` on error.
}
\description{
Returns the most recent trades for a given market from the Coinone public
API (up to 200 trades). Coinone's public endpoint does not support
cursor-based pagination, so results are limited to the latest trades that
fall within `from`–`to`.
}
\examples{
\dontrun{
get_coinone_trades(
  "BTC-KRW",
  from = as.POSIXct("2024-01-01 09:00:00", tz = "Asia/Seoul"),
  to   = as.POSIXct("2024-01-01 09:05:00", tz = "Asia/Seoul")
)
}

}
