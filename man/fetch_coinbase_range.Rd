% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coinbase.R
\name{fetch_coinbase_range}
\alias{fetch_coinbase_range}
\title{Fetch a date-range of OHLCV candles from Coinbase}
\usage{
fetch_coinbase_range(market, from, to, unit = "min")
}
\arguments{
\item{market}{Character. Market in `"ASSET-QUOTE"` format (e.g., `"BTC-USD"`).}

\item{from}{POSIXct or Date. Start of the range (inclusive).}

\item{to}{POSIXct or Date. End of the range (inclusive).}

\item{unit}{Character. Candle unit: `"min"` (60 s), `"hour"` (3600 s), or
`"day"` (86400 s). Defaults to `"min"`.}
}
\value{
A [data.frame] sorted by `time_kst` with duplicates removed, or
  `NULL` on error.
}
\description{
Iterates over time chunks (max 300 bars each) to retrieve all bars between
`from` and `to` from the Coinbase Exchange.
}
\examples{
\dontrun{
fetch_coinbase_range(
  "BTC-USD",
  from = as.POSIXct("2024-01-01 00:00:00", tz = "UTC"),
  to   = as.POSIXct("2024-01-01 01:00:00", tz = "UTC")
)
}

}
