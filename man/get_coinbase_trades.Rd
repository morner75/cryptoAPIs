% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coinbase.R
\name{get_coinbase_trades}
\alias{get_coinbase_trades}
\title{Fetch trade tick data from Coinbase over a date range}
\usage{
get_coinbase_trades(market, from, to)
}
\arguments{
\item{market}{Character. Market in `"ASSET-QUOTE"` format (e.g., `"BTC-USD"`).}

\item{from}{POSIXct or Date. Start of the range (inclusive).}

\item{to}{POSIXct or Date. End of the range (inclusive).}
}
\value{
A [data.frame] with columns `time_kst`, `trade_price`, `volume`,
  `ask_bid` (`"ASK"` = seller-initiated / `"BID"` = buyer-initiated),
  `sequential_id`, sorted by `time_kst`. Returns `NULL` on error.
}
\description{
Paginates through the Coinbase Exchange trades endpoint
(`/products/{id}/trades`) to retrieve all trades between `from` and `to`.
Uses `before` (trade ID) cursor-based pagination.
}
\examples{
\dontrun{
get_coinbase_trades(
  "BTC-USD",
  from = as.POSIXct("2024-01-01 00:00:00", tz = "Asia/Seoul"),
  to   = as.POSIXct("2024-01-01 00:05:00", tz = "Asia/Seoul")
)
}

}
