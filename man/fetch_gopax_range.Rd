% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gopax.R
\name{fetch_gopax_range}
\alias{fetch_gopax_range}
\title{Fetch a date-range of OHLCV candles from GOPAX}
\usage{
fetch_gopax_range(market, from, to, unit = "min")
}
\arguments{
\item{market}{Character. Market in `"ASSET-QUOTE"` format (e.g., `"BTC-KRW"`).}

\item{from}{POSIXct or Date. Start of the range (inclusive).}

\item{to}{POSIXct or Date. End of the range (inclusive).}

\item{unit}{Character. Candle unit: `"min"` (1-minute), `"hour"` (30-minute
approximation), or `"day"`. Defaults to `"min"`.}
}
\value{
A [data.frame] with columns `time_kst`, `opening_price`,
  `high_price`, `low_price`, `trade_price`, `volume`, sorted by `time_kst`
  with duplicates removed. Returns `NULL` on error or when no data is
  available.
}
\description{
Iterates over time chunks to retrieve all bars between `from` and `to`
from the GOPAX exchange.
}
\examples{
\dontrun{
fetch_gopax_range(
  "BTC-KRW",
  from = as.POSIXct("2024-01-01 00:00:00", tz = "Asia/Seoul"),
  to   = as.POSIXct("2024-01-01 01:00:00", tz = "Asia/Seoul")
)
}

}
